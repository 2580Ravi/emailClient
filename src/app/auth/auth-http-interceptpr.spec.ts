import { AuthHttpInterceptpr } from './auth-http-interceptpr';

describe('AuthHttpInterceptpr', () => {
  it('should create an instance', () => {
    expect(new AuthHttpInterceptpr()).toBeTruthy();
  });
});
